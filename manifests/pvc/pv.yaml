apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  namespace: app
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 300Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/pv0001"
